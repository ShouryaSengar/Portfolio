/**
 * Public surface of the header feature.
 *
 * Per .kiro/steering/structure.md, a feature is imported through its barrel and
 * nothing reaches inside its tree. ESLint enforces this with no-restricted-imports.
 */
export { Header } from './Header';
