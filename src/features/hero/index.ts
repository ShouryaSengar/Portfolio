/**
 * Public surface of the hero feature.
 *
 * Per .kiro/steering/structure.md, features are imported through their barrel and
 * nothing reaches inside the tree. ESLint enforces it.
 */
export { Hero } from './Hero';
